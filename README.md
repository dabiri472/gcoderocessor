# 🔧 G-Code Processor - پروسسور جی کد

یک برنامه ویندوزی برای پردازش و تکثیر فایل‌های G-Code در دستگاه‌های CNC.

![.NET](https://img.shields.io/badge/.NET-6.0-blue)
![Platform](https://img.shields.io/badge/Platform-Windows-lightgrey)
![License](https://img.shields.io/badge/License-MIT-green)

---

## 📋 قابلیت‌ها

- ✅ **تکثیر در محور X** - کپی کردن الگو در راستای محور X با فاصله مشخص
- ✅ **تکثیر در محور Y** - کپی کردن الگو در راستای محور Y با فاصله مشخص  
- ✅ **حالت شطرنجی** - تکثیر همزمان در هر دو محور X و Y
- ✅ **تنظیم Offset** - تعیین فاصله بین هر کپی (میلی‌متر)
- ✅ **تعداد کپی** - مشخص کردن تعداد تکرار در هر محور
- ✅ **فرمت‌های مختلف** - پشتیبانی از فرمت‌های cnc, nc, gcode, txt
- ✅ **ذخیره تنظیمات** - تنظیمات شما برای استفاده بعدی ذخیره می‌شود
- ✅ **رابط کاربری فارسی** - طراحی مدرن با پشتیبانی کامل فارسی

---

## 🖥️ اسکرین‌شات

برنامه دارای رابط کاربری تیره (Dark Theme) با بخش‌های رنگی است:

| بخش | رنگ | توضیح |
|-----|-----|-------|
| 📁 انتخاب فایل | آبی | انتخاب فایل G-Code ورودی |
| 🔄 حالت پردازش | بنفش | انتخاب نوع تکثیر |
| ⚙️ پارامترها | سبز | تنظیم Offset و تعداد |
| ▶️ شروع پردازش | سبز روشن | دکمه اجرا |
| 📋 وضعیت و لاگ | نارنجی | نمایش لاگ عملیات |

---

## 🚀 نحوه اجرا

### روش 1: اجرا با .NET Runtime (توصیه شده)

1. ابتدا [.NET 6.0 Runtime](https://dotnet.microsoft.com/download/dotnet/6.0) را نصب کنید
2. فایل `GCodeProcessor.exe` از پوشه `single` را اجرا کنید

### روش 2: اجرا از سورس کد

```bash
# کلون کردن مخزن
git clone https://github.com/dabiri472/gcoderocessor.git
cd gcoderocessor

# اجرای برنامه
dotnet run

# یا ساخت نسخه Release
dotnet build -c Release
```

### روش 3: ساخت فایل اجرایی تکی

```bash
# ساخت فایل exe تکی (نیاز به .NET Runtime)
dotnet publish -c Release /p:PublishSingleFile=true -r win-x64 --self-contained false -o ./single

# ساخت فایل exe مستقل (بدون نیاز به .NET Runtime - حجم بیشتر)
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true -o ./standalone
```

---

## 📖 راهنمای استفاده

### مرحله 1: انتخاب فایل
روی دکمه **"📂 انتخاب فایل"** کلیک کنید و فایل G-Code خود را انتخاب کنید.

### مرحله 2: انتخاب حالت پردازش
یکی از سه حالت را انتخاب کنید:
- **فقط محور X**: تکثیر فقط در راستای افقی
- **فقط محور Y**: تکثیر فقط در راستای عمودی
- **هر دو محور (شطرنجی)**: تکثیر در هر دو جهت

### مرحله 3: تنظیم پارامترها
- **Offset X/Y**: فاصله بین هر کپی (میلی‌متر)
- **تعداد X/Y**: تعداد کپی در هر محور
- **فرمت خروجی**: فرمت فایل نهایی

### مرحله 4: پردازش
روی دکمه **"▶️ شروع پردازش"** کلیک کنید. فایل خروجی در همان پوشه فایل ورودی با پسوند `_processed` ذخیره می‌شود.

---

## 📁 ساختار پروژه

```
gcoderocessor/
├── MainForm.cs           # فرم اصلی و UI
├── MainForm.Designer.cs  # طراحی فرم
├── Config.cs             # کلاس تنظیمات
├── Program.cs            # نقطه شروع برنامه
├── GCodeProcessor.csproj # فایل پروژه
├── README.md             # این فایل
├── single/               # فایل اجرایی تکی
│   └── GCodeProcessor.exe
└── publish/              # خروجی publish
```

---

## ⚙️ تنظیمات

تنظیمات برنامه در مسیر زیر ذخیره می‌شود:
```
%APPDATA%\GCodeProcessor\config.json
```

---

## 🛠️ نیازمندی‌ها

- **سیستم عامل**: Windows 10/11
- **Runtime**: .NET 6.0 یا بالاتر
- **IDE** (برای توسعه): Visual Studio 2022 یا VS Code

---

## 📝 مثال

فرض کنید یک الگوی ساده دارید و می‌خواهید آن را 3 بار در محور X با فاصله 50mm تکرار کنید:

**ورودی (input.nc):**
```gcode
G0 X0 Y0
G1 X10 Y10 F1000
G1 X20 Y0
```

**تنظیمات:**
- حالت: فقط محور X
- Offset X: 50
- تعداد X: 3

**خروجی (input_processed.nc):**
```gcode
G0 X0 Y0
G1 X10 Y10 F1000
G1 X20 Y0
G0 Z10.000
G0 X50.000 Y0
G1 X60.000 Y10 F1000
G1 X70.000 Y0
G0 Z10.000
G0 X100.000 Y0
G1 X110.000 Y10 F1000
G1 X120.000 Y0
M05
M30
```

---

## 🤝 مشارکت

از مشارکت شما استقبال می‌کنیم! لطفاً:
1. Fork کنید
2. Branch جدید بسازید
3. تغییرات خود را commit کنید
4. Pull Request ارسال کنید

---

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

---

## 👨‍💻 توسعه‌دهنده

ساخته شده با ❤️ برای جامعه CNC ایران

